# Test Failures due possible code bugs

## ECommerce.API.UnitTests.csproj - ECommerce.API.Controllers.Admin.UnitTests.AdminStoresControllerTests.GetStoreByTenantId_WithCancellationToken_PassesTokenToSender
- **Confidence**: High
- **Test File**: Host\ECommerce.API.UnitTests\Controllers\Admin\AdminStoresControllerTests.cs
- **Bug Location**: src\Host\ECommerce.API\Controllers\Admin\AdminStoresController.cs@34

### Analysis
The test is correctly verifying that the CancellationToken parameter should be passed through to the ISender.Send method. However, the production code in AdminStoresController.GetStoreByTenantId (line 34) is not passing the cancellationToken parameter to _sender.Send(). It only passes the query object, resulting in a default CancellationToken being used instead of the one provided to the method. Other methods in the same controller (e.g., GetStoreById at line 46, GetStoreBySlug, ActivateStore, etc.) correctly pass the cancellationToken, confirming this is an inconsistency/bug in the GetStoreByTenantId method.

### Suggested Fix
In AdminStoresController.cs at line 34, change:

```csharp
var store = await _sender.Send(new GetStoreByTenantIdQuery(TenantIdConverter.ToGuid(tenantId)));
```

to:

```csharp
var store = await _sender.Send(new GetStoreByTenantIdQuery(TenantIdConverter.ToGuid(tenantId)), cancellationToken);
```

This ensures the CancellationToken parameter is properly passed through to the MediatR ISender.Send method, consistent with all other methods in the controller.

## ECommerce.API.UnitTests.csproj - ECommerce.API.ExceptionHandlers.UnitTests.GlobalExceptionHandlerTests.TryHandleAsync_DeeplyNestedInnerExceptions_HandlesSuccessfully
- **Confidence**: High
- **Test File**: Host\ECommerce.API.UnitTests\ExceptionHandlers\GlobalExceptionHandlerTests.cs
- **Bug Location**: src\Host\ECommerce.API\ExceptionHandlers\GlobalExceptionHandler.cs@65-67

### Analysis
The test failure occurs because the production code sets the ContentType to 'application/problem+json' at line 65, but then calls WriteAsJsonAsync at line 67 which overwrites the ContentType with 'application/json; charset=utf-8'. The WriteAsJsonAsync method automatically sets the ContentType header, overriding the previously set value. The test correctly expects 'application/problem+json' per RFC 9457 (Problem Details for HTTP APIs) specification.

### Suggested Fix
In GlobalExceptionHandler.cs, the ContentType should be set AFTER calling WriteAsJsonAsync, not before. Change lines 64-67 to:

```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;

await httpContext.Response.WriteAsJsonAsync(problemDetails, cancellationToken);

httpContext.Response.ContentType = "application/problem+json";
```

Alternatively, you could use JsonSerializer.SerializeAsync with a configured stream to avoid the automatic ContentType setting:

```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;
httpContext.Response.ContentType = "application/problem+json";

await System.Text.Json.JsonSerializer.SerializeAsync(
    httpContext.Response.Body,
    problemDetails,
    new System.Text.Json.JsonSerializerOptions { PropertyNamingPolicy = System.Text.Json.JsonNamingPolicy.CamelCase },
    cancellationToken);
```

## ECommerce.API.UnitTests.csproj - ECommerce.API.ExceptionHandlers.UnitTests.GlobalExceptionHandlerTests.TryHandleAsync_DevelopmentEnvironmentWithInnerException_ReturnsTrueAndIncludesDebugInfo
- **Confidence**: High
- **Test File**: Host\ECommerce.API.UnitTests\ExceptionHandlers\GlobalExceptionHandlerTests.cs
- **Bug Location**: src\Host\ECommerce.API\ExceptionHandlers\GlobalExceptionHandler.cs@65

### Analysis
The production code sets ContentType to 'application/problem+json' on line 65, but then calls WriteAsJsonAsync on line 67. The WriteAsJsonAsync extension method from ASP.NET Core always sets the ContentType to 'application/json; charset=utf-8', which overwrites the previously set value. The test correctly expects 'application/problem+json' according to RFC 9457 (Problem Details for HTTP APIs). The production code needs to set the ContentType AFTER calling WriteAsJsonAsync to ensure it's not overwritten.

### Suggested Fix
In GlobalExceptionHandler.cs, the ContentType should be set AFTER the WriteAsJsonAsync call to prevent it from being overwritten. Move line 65 to after line 67:

```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;

await httpContext.Response.WriteAsJsonAsync(problemDetails, cancellationToken);

httpContext.Response.ContentType = "application/problem+json";

return true;
```

This ensures that the ContentType is set to the correct RFC 9457 compliant value after WriteAsJsonAsync has written the JSON response.

## ECommerce.API.UnitTests.csproj - ECommerce.API.ExceptionHandlers.UnitTests.GlobalExceptionHandlerTests.TryHandleAsync_DevelopmentEnvironmentWithoutInnerException_ReturnsTrueAndProcessesSuccessfully
- **Confidence**: High
- **Test File**: Host\ECommerce.API.UnitTests\ExceptionHandlers\GlobalExceptionHandlerTests.cs
- **Bug Location**: src\Host\ECommerce.API\ExceptionHandlers\GlobalExceptionHandler.cs@65-67

### Analysis
The production code at line 65 sets the ContentType to 'application/problem+json', but the subsequent call to WriteAsJsonAsync at line 67 overwrites it with the default 'application/json; charset=utf-8'. The WriteAsJsonAsync extension method internally sets the ContentType header, which overrides the manually set value. The test is correct in expecting 'application/problem+json' as this is the standard media type for RFC 9457 Problem Details for HTTP APIs.

### Suggested Fix
The production code needs to be fixed to ensure the ContentType remains 'application/problem+json'. The recommended fix is to move the ContentType assignment after calling WriteAsJsonAsync, or use a lower-level API that doesn't override the ContentType. Here's the recommended fix:

```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;

await httpContext.Response.WriteAsJsonAsync(problemDetails, cancellationToken);

// WriteAsJsonAsync sets ContentType to application/json, so we override it
httpContext.Response.ContentType = "application/problem+json";
```

Alternatively, use JsonSerializer directly:

```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;
httpContext.Response.ContentType = "application/problem+json";

await JsonSerializer.SerializeAsync(httpContext.Response.Body, problemDetails, 
    new JsonSerializerOptions { PropertyNamingPolicy = JsonNamingPolicy.CamelCase }, 
    cancellationToken);
```

## ECommerce.API.UnitTests.csproj - ECommerce.API.ExceptionHandlers.UnitTests.GlobalExceptionHandlerTests.TryHandleAsync_DifferentExceptionTypes_HandlesAllSuccessfully
- **Confidence**: High
- **Test File**: Host\ECommerce.API.UnitTests\ExceptionHandlers\GlobalExceptionHandlerTests.cs
- **Bug Location**: src\Host\ECommerce.API\ExceptionHandlers\GlobalExceptionHandler.cs@65-67

### Analysis
The test correctly expects the ContentType to be 'application/problem+json' for ProblemDetails responses, which follows RFC 9457 standards. The production code explicitly sets this on line 65, but the subsequent call to WriteAsJsonAsync on line 67 overrides it to 'application/json; charset=utf-8'. This is a known behavior of the WriteAsJsonAsync extension method in ASP.NET Core. The production code needs to be fixed to properly set and maintain the correct content type.

### Suggested Fix
In GlobalExceptionHandler.cs at line 65-67, the code needs to be modified to preserve the 'application/problem+json' content type. The recommended fix is to use one of these approaches:

**Option 1: Manual serialization (Recommended)**
```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;
httpContext.Response.ContentType = "application/problem+json";

await JsonSerializer.SerializeAsync(
    httpContext.Response.Body,
    problemDetails,
    problemDetails.GetType(),
    cancellationToken: cancellationToken);
```

**Option 2: Use Results.Json (Alternative)**
```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;
await httpContext.Response.WriteAsync(
    JsonSerializer.Serialize(problemDetails),
    cancellationToken);
httpContext.Response.ContentType = "application/problem+json";
```

Note: You'll need to add `using System.Text.Json;` at the top of the file for Option 1 or 2.

**Option 3: Use TypedResults (ASP.NET Core 7+)**
```csharp
var result = TypedResults.Problem(
    detail: problemDetails.Detail,
    instance: problemDetails.Instance,
    statusCode: problemDetails.Status,
    title: problemDetails.Title,
    type: problemDetails.Type,
    extensions: problemDetails.Extensions);

await result.ExecuteAsync(httpContext);
```

The issue is that WriteAsJsonAsync always sets the content type to 'application/json; charset=utf-8' regardless of any previously set value.

## ECommerce.API.UnitTests.csproj - ECommerce.API.ExceptionHandlers.UnitTests.GlobalExceptionHandlerTests.TryHandleAsync_DifferentHttpMethods_HandlesAllSuccessfully
- **Confidence**: High
- **Test File**: Host\ECommerce.API.UnitTests\ExceptionHandlers\GlobalExceptionHandlerTests.cs
- **Bug Location**: src\Host\ECommerce.API\ExceptionHandlers\GlobalExceptionHandler.cs@65-67

### Analysis
The test correctly expects the ContentType to be 'application/problem+json', and the production code explicitly sets it at line 65. However, the subsequent call to WriteAsJsonAsync at line 67 overwrites the ContentType header to 'application/json; charset=utf-8'. This is a known behavior of WriteAsJsonAsync - it always sets the ContentType to 'application/json; charset=utf-8'. The fix is to set the ContentType AFTER calling WriteAsJsonAsync, or use a different serialization approach that doesn't override the ContentType.

### Suggested Fix
In GlobalExceptionHandler.cs, the ContentType should be set AFTER calling WriteAsJsonAsync to prevent it from being overwritten. Change lines 65-67 to:

```csharp
await httpContext.Response.WriteAsJsonAsync(problemDetails, cancellationToken);
httpContext.Response.ContentType = "application/problem+json";
```

Alternatively, you can set the StatusCode before WriteAsJsonAsync and ContentType after:

```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;
await httpContext.Response.WriteAsJsonAsync(problemDetails, cancellationToken);
httpContext.Response.ContentType = "application/problem+json";
```

This ensures that the ContentType remains as 'application/problem+json' after the JSON response is written.

## ECommerce.API.UnitTests.csproj - ECommerce.API.ExceptionHandlers.UnitTests.GlobalExceptionHandlerTests.TryHandleAsync_EmptyTraceIdentifier_HandlesSuccessfully
- **Confidence**: High
- **Test File**: Host\ECommerce.API.UnitTests\ExceptionHandlers\GlobalExceptionHandlerTests.cs
- **Bug Location**: src\Host\ECommerce.API\ExceptionHandlers\GlobalExceptionHandler.cs@68-70

### Analysis
The production code at line 68 correctly sets the ContentType to 'application/problem+json', but then line 70 calls WriteAsJsonAsync which internally overwrites the ContentType to 'application/json; charset=utf-8'. This is a known behavior of WriteAsJsonAsync. The test correctly expects 'application/problem+json' which is the standard content type for Problem Details (RFC 7807). The production code needs to be fixed to either: 1) Set ContentType after the JSON is written (not possible), 2) Use JsonSerializer.SerializeAsync directly instead of WriteAsJsonAsync, or 3) Rearrange the code so that ContentType is set in a way that won't be overwritten.

### Suggested Fix
In GlobalExceptionHandler.cs at lines 68-70, replace the ContentType setting and WriteAsJsonAsync call with code that won't overwrite the ContentType. The recommended fix is to use System.Text.Json.JsonSerializer.SerializeAsync directly:

```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;
httpContext.Response.ContentType = "application/problem+json";

await System.Text.Json.JsonSerializer.SerializeAsync(
    httpContext.Response.Body, 
    problemDetails, 
    problemDetails.GetType(),
    cancellationToken: cancellationToken);
```

Alternatively, to maintain similar behavior to WriteAsJsonAsync with proper JSON options, retrieve the JsonOptions from the service provider and use them:

```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;
httpContext.Response.ContentType = "application/problem+json";

var jsonOptions = httpContext.RequestServices
    .GetService<Microsoft.Extensions.Options.IOptions<Microsoft.AspNetCore.Http.Json.JsonOptions>>()?.
    Value?.SerializerOptions ?? new System.Text.Json.JsonSerializerOptions();

await System.Text.Json.JsonSerializer.SerializeAsync(
    httpContext.Response.Body, 
    problemDetails,
    jsonOptions,
    cancellationToken);
```

## ECommerce.API.UnitTests.csproj - ECommerce.API.ExceptionHandlers.UnitTests.GlobalExceptionHandlerTests.TryHandleAsync_ExceptionMessageWithSpecialCharacters_HandlesSuccessfully
- **Confidence**: High
- **Test File**: Host\ECommerce.API.UnitTests\ExceptionHandlers\GlobalExceptionHandlerTests.cs
- **Bug Location**: src\Host\ECommerce.API\ExceptionHandlers\GlobalExceptionHandler.cs@67

### Analysis
The test correctly expects the ContentType to be 'application/problem+json' according to RFC 9457 (Problem Details for HTTP APIs). The production code sets this value on line 68 (actual line 65), but the subsequent call to WriteAsJsonAsync on line 70 (actual line 67) overwrites it to 'application/json; charset=utf-8'. The WriteAsJsonAsync extension method automatically sets the ContentType header, overriding the previously set value. The fix is to set the ContentType after calling WriteAsJsonAsync, or to use manual JSON serialization with WriteAsync instead.

### Suggested Fix
The issue is in GlobalExceptionHandler.cs at line 67. The WriteAsJsonAsync method overwrites the ContentType that was set on line 65. The fix is to move the ContentType assignment AFTER the WriteAsJsonAsync call:

```csharp
await httpContext.Response.WriteAsJsonAsync(problemDetails, cancellationToken);
httpContext.Response.ContentType = "application/problem+json";
```

Alternatively, to be more explicit and avoid any potential header locking issues, manually serialize and write the response:

```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;
httpContext.Response.ContentType = "application/problem+json";

var json = System.Text.Json.JsonSerializer.Serialize(problemDetails, new System.Text.Json.JsonSerializerOptions
{
    PropertyNamingPolicy = System.Text.Json.JsonNamingPolicy.CamelCase
});

await httpContext.Response.WriteAsync(json, cancellationToken);
```

## ECommerce.API.UnitTests.csproj - ECommerce.API.ExceptionHandlers.UnitTests.GlobalExceptionHandlerTests.TryHandleAsync_ExceptionWithNullStackTrace_HandlesSuccessfully
- **Confidence**: High
- **Test File**: Host\ECommerce.API.UnitTests\ExceptionHandlers\GlobalExceptionHandlerTests.cs
- **Bug Location**: src\Host\ECommerce.API\ExceptionHandlers\GlobalExceptionHandler.cs@67

### Analysis
The test expects the ContentType to be 'application/problem+json', which is the correct content type for RFC 7807 Problem Details. The production code sets this ContentType on line 65, but then calls WriteAsJsonAsync on line 67, which overwrites the ContentType to 'application/json; charset=utf-8'. This is a known behavior of the WriteAsJsonAsync extension method. The test expectation is correct per the RFC 7807 standard for Problem Details.

### Suggested Fix
The GlobalExceptionHandler.TryHandleAsync method needs to be fixed to preserve the 'application/problem+json' content type after writing the JSON response. There are two recommended approaches:

**Option 1: Set ContentType after WriteAsJsonAsync**
```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;

await httpContext.Response.WriteAsJsonAsync(problemDetails, cancellationToken);

httpContext.Response.ContentType = "application/problem+json";
```

**Option 2: Use JsonSerializer directly (preferred for better control)**
```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;
httpContext.Response.ContentType = "application/problem+json";

await JsonSerializer.SerializeAsync(
    httpContext.Response.Body,
    problemDetails,
    problemDetails.GetType(),
    cancellationToken: cancellationToken);
```

Option 1 is simpler and requires minimal code changes. Option 2 is more explicit and respects the ContentType set before serialization. Both will ensure the ContentType remains 'application/problem+json' as required by the RFC 7807 standard.

## ECommerce.API.UnitTests.csproj - ECommerce.API.ExceptionHandlers.UnitTests.GlobalExceptionHandlerTests.TryHandleAsync_ProductionEnvironment_ReturnsTrueAndExcludesDebugInfo
- **Confidence**: High
- **Test File**: Host\ECommerce.API.UnitTests\ExceptionHandlers\GlobalExceptionHandlerTests.cs
- **Bug Location**: src\Host\ECommerce.API\ExceptionHandlers\GlobalExceptionHandler.cs@65-67

### Analysis
The test correctly expects the Content-Type to be 'application/problem+json' according to RFC 7807 (Problem Details for HTTP APIs) standard. The production code sets this content type on line 65, but then calls WriteAsJsonAsync on line 67 which internally overrides the Content-Type to 'application/json; charset=utf-8'. This is a known behavior of WriteAsJsonAsync - it sets or overrides the content type. The production code needs to be modified to preserve the correct Problem Details content type.

### Suggested Fix
The GlobalExceptionHandler.TryHandleAsync method needs to be fixed at lines 65-67. There are several possible fixes:

**Option 1 (Recommended)**: Use JsonSerializer.SerializeToStreamAsync directly with the correct content type:
```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;
httpContext.Response.ContentType = "application/problem+json";

await JsonSerializer.SerializeAsync(httpContext.Response.Body, problemDetails, 
    new JsonSerializerOptions { PropertyNamingPolicy = JsonNamingPolicy.CamelCase }, 
    cancellationToken);
```

**Option 2**: Set the content type after WriteAsJsonAsync:
```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;

await httpContext.Response.WriteAsJsonAsync(problemDetails, cancellationToken);

httpContext.Response.ContentType = "application/problem+json";
```
Note: This might not work in all scenarios if the response has already been sent.

**Option 3**: Use TypedResults.Problem or Results.Json with custom options (if available in the ASP.NET Core version being used).

The root cause is that WriteAsJsonAsync always sets Content-Type to 'application/json; charset=utf-8', overriding any previously set value.

## ECommerce.API.UnitTests.csproj - ECommerce.API.ExceptionHandlers.UnitTests.GlobalExceptionHandlerTests.TryHandleAsync_VeryLongExceptionMessage_HandlesSuccessfully
- **Confidence**: High
- **Test File**: Host\ECommerce.API.UnitTests\ExceptionHandlers\GlobalExceptionHandlerTests.cs
- **Bug Location**: src\Host\ECommerce.API\ExceptionHandlers\GlobalExceptionHandler.cs@65-67

### Analysis
The production code sets httpContext.Response.ContentType to 'application/problem+json' on line 65, but then calls WriteAsJsonAsync on line 67, which overwrites the ContentType to 'application/json; charset=utf-8'. This is a known behavior of the WriteAsJsonAsync extension method. The test correctly expects 'application/problem+json' as per RFC 7807 Problem Details specification. The production code needs to be fixed to preserve the correct ContentType.

### Suggested Fix
The GlobalExceptionHandler.TryHandleAsync method needs to be fixed at line 65-67 in C:\Users\gorke\source\repos\ECommerce\src\Host\ECommerce.API\ExceptionHandlers\GlobalExceptionHandler.cs. The issue is that WriteAsJsonAsync overwrites the ContentType. The fix is to either: (1) Move the ContentType assignment after WriteAsJsonAsync, or (2) Use WriteAsync with JsonSerializer.SerializeToStreamAsync to maintain control over the ContentType. Recommended fix:

```csharp
httpContext.Response.StatusCode = StatusCodes.Status500InternalServerError;

await httpContext.Response.WriteAsJsonAsync(problemDetails, cancellationToken);

httpContext.Response.ContentType = "application/problem+json";
```

This ensures the ContentType is set after WriteAsJsonAsync completes, preserving the correct 'application/problem+json' content type for RFC 7807 Problem Details responses.

